services:
  # ─── Infraestrutura ────────────────────────────────────────────
  mariadb:
    image: mariadb:lts
    container_name: cria-viva-db
    environment:
      MARIADB_USER: criaviva
      MARIADB_PASSWORD: criaviva123
      MARIADB_DATABASE: criaviva_dev
      MARIADB_ROOT_PASSWORD: root123
    ports:
      - '3306:3306'
    volumes:
      - mariadb_data:/var/lib/mysql
    healthcheck:
      test: ['CMD', 'healthcheck.sh', '--connect', '--innodb_initialized']
      interval: 5s
      timeout: 5s
      retries: 15

  redis:
    image: redis:7-alpine
    container_name: cria-viva-redis
    # Porta não exposta ao host — API acessa via rede interna Docker
    volumes:
      - redis_data:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 5s
      timeout: 3s
      retries: 10

  # ─── Aplicação ─────────────────────────────────────────────────
  api:
    container_name: cria-viva-api
    build:
      context: .
      dockerfile: apps/api/Dockerfile
      target: dev
    ports:
      - '3002:3001'
    environment:
      DATABASE_URL: mysql://criaviva:criaviva123@mariadb:3306/criaviva_dev
      REDIS_HOST: redis
      REDIS_PORT: 6379
      JWT_SECRET: dev-secret-local
      FRONTEND_URL: http://localhost:3001
      PORT: 3001
      # Chaves VAPID vêm do .env (gitignored) — gere as suas com:
      #   npx web-push generate-vapid-keys
      # Sem elas a API sobe normalmente, apenas com push desabilitado.
      VAPID_PUBLIC_KEY: ${VAPID_PUBLIC_KEY:-}
      VAPID_PRIVATE_KEY: ${VAPID_PRIVATE_KEY:-}
      VAPID_EMAIL: ${VAPID_EMAIL:-mailto:dev@localhost}
      CRON_SECRET: criaviva-cron-dev-2026
    volumes:
      - ./packages:/app/packages
      - ./apps/api/src:/app/apps/api/src
      - ./apps/api/prisma:/app/apps/api/prisma
    depends_on:
      mariadb:
        condition: service_healthy
      redis:
        condition: service_healthy

  web:
    container_name: cria-viva-web
    build:
      context: .
      dockerfile: apps/web/Dockerfile
      target: dev
    ports:
      - '3001:3000'
    environment:
      # Dentro do Docker o proxy aponta para o serviço "api" via rede interna
      VITE_API_TARGET: http://api:3001
    volumes:
      - ./packages:/app/packages
      - ./apps/web/src:/app/apps/web/src
      - ./apps/web/public:/app/apps/web/public
      - ./apps/web/index.html:/app/apps/web/index.html
    depends_on:
      - api

volumes:
  mariadb_data:
  redis_data:
